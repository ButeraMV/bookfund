<h1>Browse our eBooks</h1>

<% @ebooks.each do |ebook| %>
  <div class="ebook">
    <div class="title">
      <%= ebook.title %>
    </div>
    <div class="description">
      <%= ebook.description %>
    </div>
  </div>
<% end %>

<%= will_paginate @posts %>